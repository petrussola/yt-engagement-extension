{
  "name": "youtube-engage-o-meter",
  "version": "1.0.0",
  "description": "Browser extension that highlights YouTube videos with unusually low visible engagement for their view count.",
  "type": "module",
  "scripts": {
    "build": "vite build",
    "dev": "vite build --watch",
    "firefox:zip": "npm run build && mkdir -p artifacts && cd dist && zip -r ../artifacts/youtube-engage-o-meter-firefox.zip .",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "eslint .",
    "test": "tsc -p tsconfig.test.json && node --test .test-dist/engagement.test.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@eslint/js": "^9.39.4",
    "@types/chrome": "^0.1.40",
    "@types/node": "^25.6.0",
    "eslint": "^9.39.4",
    "prettier": "^3.8.3",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.59.1",
    "vite": "^8.0.10"
  }
}
